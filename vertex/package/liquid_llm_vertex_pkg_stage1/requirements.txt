# --- Core runtime ---
torch==2.4.0
transformers>=4.40.0
huggingface_hub>=0.23.0
numpy>=1.23
pyyaml>=6.0.1
tqdm>=4.66.0
tokenizers>=0.15.0

# --- GCS I/O fallback (when gcloud isn't available) ---
gcsfs>=2024.1.0

# --- Optional parquet support (only used if you read Parquet directly) ---
pyarrow>=15.0.0

# --- Testing ---
pytest>=7.4.0

# NOTE: Do NOT list FlashAttention here; it's installed from your GCS wheel at runtime.
