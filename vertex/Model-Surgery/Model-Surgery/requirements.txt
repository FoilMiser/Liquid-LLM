# Core deep learning stack (CUDA 12.1 build)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
# install from the PyTorch cu121 wheel index:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu121

# Numeric and utility libs
numpy==1.26.4
tqdm==4.66.4
pyyaml==6.0.2
einops==0.8.0

# Jupyter & notebook utilities
ipykernel>=6.29.5
jupyterlab>=4.2.5

# Optional helpers (safe to omit if not needed)
pandas>=2.2.2
matplotlib>=3.9.2
